<%= render :partial => 'item',
  :collection => @items, 
  :as => :item
  %>

<div class="hlisting">
  <%= will_paginate @items %>
</div>

<% if @items.empty? %>
  Sorry! We don't have anything matching "<%= params[:term] %>".
<% end %>
